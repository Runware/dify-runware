[project]
name = "dify-runware"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin>=0.9.1,<0.10.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
